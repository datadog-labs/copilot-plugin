{
  "mcpServers": {
    "datadog": {
      "type": "http",
      "url": "https://not-setup/v1/mcp?referrer_ide=copilot-plugin&plugin_version=0.7.16&toolsets=core",
      "headers": {
        "DD_API_KEY": "${env:DD_API_KEY}",
        "DD_APPLICATION_KEY": "${env:DD_APPLICATION_KEY}"
      }
    }
  }
}
